// Generated by CoffeeScript 1.10.0
var Sizzle, root, touchManager;

Sizzle = require('Sizzle');

root = Sizzle('body')[0];

touchManager = {
  "_startX": void 0,
  "_startY": void 0,
  "touchstart": (function(_this) {
    return function(event) {
      _this._startX = event.touches[0].pageX;
      return _this._startY = event.touches[0].pageY;
    };
  })(this),
  "touchmove": (function(_this) {
    return function(event) {
      var moveX, moveY;
      moveX = Math.abs(_this._startX - event.touches[0].pageX);
      moveY = Math.abs(_this._startY - event.touches[0].pageY);
      if (moveY > moveX) {
        return event.preventDefault();
      }
    };
  })(this)
};

root.addEventListener('touchstart', touchManager.touchstart);

root.addEventListener('touchmove', touchManager.touchmove);

Sizzle('#join')[0].addEventListener('click', function() {
  return jsApiCall();
});

Sizzle('.active_code>.button')[0].addEventListener('click', function(event) {
  return alert('验证码错误');
});


	function jsApiCall() {
		WeixinJSBridge.invoke('getBrandWCPayRequest', {{prepay_content}}, function(res){});
	}

	//dont allow share
	function onBridgeReady(){
		WeixinJSBridge.call('hideOptionMenu');
	}

	if (typeof WeixinJSBridge == "undefined"){
		if( document.addEventListener ){
			document.addEventListener('WeixinJSBridgeReady', onBridgeReady, false);
		}else if (document.attachEvent){
			document.attachEvent('WeixinJSBridgeReady', onBridgeReady); 
			document.attachEvent('onWeixinJSBridgeReady', onBridgeReady);
		}
	}else{
		onBridgeReady();
	}
;
